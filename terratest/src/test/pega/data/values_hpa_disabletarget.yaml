---
global:
  tier:
    - name: "web"
      hpa:
        enabled: true
        cpuTarget: "off"
        memoryTarget: "on"
    - name: "batch"
      hpa:
        enabled: true
        # cpuTarget should be "on" as default
        memoryTarget: "off"