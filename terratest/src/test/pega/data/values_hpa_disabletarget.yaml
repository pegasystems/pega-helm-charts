---
global:
  tier:
    - name: "web"
      hpa:
        enabled: true
        enableCpuTarget: false
    - name: "batch"
      hpa:
        enabled: true
        # enableCpuTarget should be true as default
