{{ if or (eq (include "performInstallAndDeployment" .) "true") (eq (include "performUpgradeAndDeployment" .) "true") (eq (include "isZeroDowntimeUpgrdeInInstaller" .) "true") }}
apiVersion: rbac.authorization.k8s.io/v1beta1
kind: Role
metadata:
  name: {{ template "installerJobReaderRole" }}
  namespace: {{ .Release.Namespace }}
rules:
- apiGroups: ["apps"]
  resources: ["deployments", "statefulsets"]
  verbs: ["get", "watch", "list"]
- apiGroups: ["batch"]
  resources: ["jobs"]
  verbs: ["get", "watch", "list"]
- apiGroups: ["extensions"]
  resources: ["deployments"]
  verbs: ["get", "watch", "list"]
{{ end }}
