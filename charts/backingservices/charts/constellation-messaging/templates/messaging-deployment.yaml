{{- if .Values.enabled }}
kind: Deployment
apiVersion: apps/v1
metadata:
  name: {{ .Values.name }}
  labels:
    app: {{ .Values.name }}
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.name }}
    spec:
      imagePullSecrets:
      {{- range .Values.imagePullSecretNames }}
        - name: {{ . }}
      {{- end }}
      containers:
      - name: c11n-messaging
        imagePullPolicy: {{ .Values.imagePullPolicy }}
        image: {{ .Values.image }}
        resources:
          {{ if and .Values.resources .Values.resources.requests .Values.resources.requests.memory }}
          requests:
            memory: {{ .Values.resources.requests.memory }}
          {{- end }}
          {{ if and .Values.resources .Values.resources.limits .Values.resources.limits.memory }}
          limits:
            memory: {{ .Values.resources.limits.memory }}
          {{- end }}
        securityContext:
          seccompProfile:
            type: {{ .Values.securityContext.seccompProfile.type }}
          readOnlyRootFilesystem: {{ .Values.securityContext.readOnlyRootFilesystem }}
          allowPrivilegeEscalation: {{ .Values.securityContext.allowPrivilegeEscalation }}
        args:
          - --max-semi-space-size=1024
          - port={{ .Values.pegaMessagingTargetPort }}
          - path=/c11n-messaging
        ports:
        - containerPort: {{ .Values.pegaMessagingTargetPort }}
{{ end }}
